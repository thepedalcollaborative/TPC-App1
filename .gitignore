# Local secrets and private keys
P8/*.p8
*.p8

# Claude Code local settings (may contain tokens/keys)
.claude/settings.local.json

# Local env (real values — never commit)
.env
.env.*
!.env.example

# macOS
.DS_Store

# Node
node_modules/

# Expo
.expo/
